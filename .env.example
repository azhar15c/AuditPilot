HF_TOKEN=your_hf_token_here
GROQ_API_KEY=your_groq_api_key_here

# Guidewire PolicyCenter integration (optional — leave blank to use mock mode)
GW_PC_BASE_URL=https://your-tenant.guidewire.net/pc/rest/v1
GW_PC_USERNAME=your_api_user
GW_PC_PASSWORD=your_api_password

# Demo-safe mode (optional) — set to 1/true to run the sample audit with
# realistic canned model responses instead of live Groq/HF calls. Use this
# for live demos where Groq's free-tier rate limits can't be relied on.
# See demo_fixtures.py.
AUDITPILOT_DEMO_MODE=
